# Error Registry

Structured errors with root causes and fixes. Newest first.
Updated after every bugfix via `/docs` or error-learning rule.

See also: `.claude/data/error-log.md` (Claude Code internal log).

<!--
## YYYY-MM-DD — [category] Short description

**Symptom:** what happened (user-visible behavior)
**Root Cause:** why it happened (dig deep, not the surface)
**Fix:** exact code/config change
**Prevention:** how to avoid in future
**Files:** affected files

Categories: [deploy], [config], [docker], [auth], [api], [db], [ui], [logic]
-->
